# Crypto Flask Helm Chart

This Helm chart allows you to deploy a Flask application using Kubernetes.

## Prerequisites

- Kubernetes cluster
- Helm installed

## Installation

1. Clone this repository:

   ```sh
   git clone <repository_url>
